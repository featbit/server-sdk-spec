# Shared conformance fixtures

This directory is reserved for language-neutral evaluation, rollout, and protocol fixtures. No executable shared fixture suite is included in the initial migration. Existing examples and rollout vectors remain in the specification.

Future fixtures should be versioned with the specification and consumed by each SDK using its native test framework. See [conformance requirements](../spec/conformance.md).
